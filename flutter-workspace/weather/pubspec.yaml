name: weather
description: The Flutter Test
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0-69.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^0.13.5
  fl_chart: ^0.62.0
  math_expressions: ^2.4.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true