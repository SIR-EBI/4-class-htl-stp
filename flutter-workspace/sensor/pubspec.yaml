name: sensor
description: A Flutter sensor app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0-69.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^3.0.1

  cupertino_icons: ^1.0.2
  flutter_colorpicker: ^1.0.3
  flutter_inset_box_shadow: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/circle.png
